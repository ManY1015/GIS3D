# GIS3D 项目使用说明�?
## 1. 程序启动
- 可执行文件路径：`D:\专业学习\三维GIS\实习三\GIS3D3\GIS3D3\bin-x64\Gis3Dd.exe`
- 启动后默认显示天空背景、绿地背景和右侧“图层管理”面板�?- 主要操作入口在菜单栏 `实验`�?- 右侧图层面板用于显示当前已加载图层，并支持显隐、透明度、删除、上下移动和右键属性编辑�?
## 2. 加载单个模型
### 功能用�?- 加载单个三维模型到当�?OSG 场景�?
### 支持格式
- 文件对话框显式筛选：`*.osgb`
- 底层读取方式：`osgDB::readNodeFile(...)`
- 理论上可支持已安�?OSG 插件对应的其他格式，但界面默认只引导 `osgb`�?
### 操作步骤
1. 点击 `实验 -> 加载单个模型`
2. 在文件对话框中选择一个模型文�?3. 点击打开

### 成功标志
- 场景中出现模�?- 图层管理面板新增一行图层（默认名称�?`Model`�?
### 常见问题
- 如果文件对话框看不到 `obj/osg` 等格式，是因为过滤器只显�?`osgb`
- 如果模型加载后看不到，可能是模型本身坐标范围较大或视角未定位到模型附�?- 当前工程没有内置“宋卿体育馆单模型”示例资源，需要用户自己提供模型文�?
## 3. 加载批量模型
### 功能用�?- 一次选择多个模型文件，批量加入场景�?
### 操作步骤
1. 点击 `实验 -> 加载批量模型`
2. 在文件对话框中按�?`Ctrl` �?`Shift` 多选模型文�?3. 点击打开

### 成功/失败统计怎么�?- 程序会弹�?`AfxMessageBox`
- 显示总文件数、成功数、失败数
- 如果有失败，最多列出前 5 个失败文件名

### 成功标志
- 多个模型被加入场�?- 图层面板出现多行模型图层

### 常见问题
- 文件对话框同样只显式筛�?`*.osgb`
- 这是“通用批量模型加载”，不是专门写死适配“宋卿体育馆模型集”的导入�?- 若批量模型图层名依赖编码字符串，极少数情况下批量总览功能可能受图层命名影�?
## 4. 加载地形与影�?### DEM 文件要求
- 代码允许选择：`*.tif; *.tiff; *.img; *.png; *.jpg; *.bmp`
- 实际最合理的是具有高程意义�?DEM 栅格

### 影像文件要求
- 可�?- 允许选择：`*.tif; *.tiff; *.jpg; *.jpeg; *.png; *.bmp`

### 操作步骤
1. 点击 `实验 -> 加载地形与影像`
2. 第一步选择 DEM 文件
3. 第二步可选选择影像文件
4. 程序开始构建地形并加入场景

### 成功标志
- 成功时会弹出�?  - �?DEM：`No image selected. DEM terrain only.`
  - DEM + 影像：`Terrain and image loaded successfully.`
- 图层管理面板新增 `地形` 图层
- 场景出现地形网格；若提供影像，应看到影像贴在地形表面

### 如何判断是否完成 DEM 与影像叠�?- 地形不只是纯高程颜色，而是出现真实影像纹理
- 旋转视角后影像仍跟随地形表面，不应漂�?
### 常见问题
- 如果影像未选，则只显示 DEM 地形
- 如果 DEM 读取失败，会弹出失败消息
- 若影像与 DEM 空间范围不一致，可能出现贴图错位

## 5. 图层管理
### 已有功能
- 图层显示/隐藏
- 透明度滑�?- 删除图层
- 上移/下移
- 右键“修改名�?透明�?..�?
### 使用方法
1. 先加载模型或地形
2. 在右侧图层面板选中图层
3. 使用复选框、滑条和按钮操作

### 注意事项
- 图层顺序调整会发送场景重排命�?- 删除图层会从面板和场景中移除

## 6. 批量模型总览
### 是否需要先批量加载模型
- 需�?
### 点击后会发生什�?- 程序会扫�?root 下名称符合“批量模型图层命名规则”的图层
- 计算整体包围�?- 将相机移动到总览位置

### 成功标志
- 视角移动到可看见整批模型的位�?
### 常见问题
- 如果没有批量模型图层，功能会失败
- 该功能依赖批量加载时生成的图层名�?
## 7. 视角书签
### 保存视角书签怎么�?1. 把相机移动到希望保存的视�?2. 点击 `实验 -> 保存视角书签`

### 恢复视角书签怎么�?1. 点击 `实验 -> 恢复视角书签`

### 是否跨程序保�?- �?- 当前保存在内存变�?`ViewBookmark` 中，不写入文�?
### 其他说明
- 未先保存就恢复，会失�?- 当前成功/失败提示主要写入视图状态文本接口，不一定总能看到显式弹窗

## 8. 对象包围盒与属性查�?### 前置条件
- 必须先加载至少一个模型或地形

### 操作步骤
1. 点击 `实验 -> 对象包围盒与属性查询`
2. 进入查询模式
3. 用鼠标左键点击场景中的一个对�?
### 输出内容
- 会绘制对象包围盒覆盖�?- 会弹出消息框，显示：
  - Layer 名称
  - Center
  - Radius

### 没反应时的原�?- 没有加载任何对象
- 没点击到场景中的对象
- 该功能是“切换为查询模式”，不是点菜单就立刻弹窗

## 9. 地形高程查询
### 前置条件
- 必须先加�?DEM 地形

### 操作步骤
1. 点击 `实验 -> 地形高程查询`
2. 进入查询模式
3. 用鼠标左键点击地形表面某一�?
### 输出位置
- 成功时通过 `AfxMessageBox` 弹窗显示�?  - Layer
  - XY
  - Elevation

### 为什么直接点击菜单可能没反应
- 该菜单不会直接弹窗，它只是切换查询模�?- 当前代码把状态提示写�?`UpdateTerrainStatus(...)`
- 但地形状态小窗口实际并没有创建，所以用户可能看不到“已进入查询模式”的反馈

### 如何验证该功能是否有�?1. 先加�?DEM
2. 点击菜单
3. 再用左键点击地形
4. 如果点到的是地形层，会弹出高程信�?
## 10. 线框检查模�?### 功能用�?- 将场景中已有命名图层切换为线框显示，便于检查网�?
### 操作步骤
1. 先加载模型或地形
2. 点击 `实验 -> 线框检查模式`
3. 再次点击可切回实体模�?
### 如何恢复
- 同一菜单再次点击即可恢复

### 成功标志
- 已加载图层从填充变为线框，或从线框恢复为实体

## 11. 导出场景截图
### 操作步骤
1. 点击 `实验 -> 导出场景截图`
2. 选择保存路径
3. 确认导出

### 保存路径
- 用户在保存对话框中自行选择

### 文件格式
- 对话框支�?`bmp` �?`png`
- 默认文件名：`scene_capture.bmp`

### 常见问题
- 该功能抓取的是当前窗口客户区图像
- 不需要一定先加载模型或地形，但空场景也会被导�?
## 12. 推荐验收流程
1. 启动程序
2. `实验 -> 加载单个模型`
3. `实验 -> 加载批量模型`
4. `实验 -> 加载地形与影像`
5. 在右侧图层管理中测试显隐、透明度、删除、上下移�?6. 测试批量模型总览、视角书签、对象包围盒与属性查询、地形高程查询、线框检查模式、导出场景截�?7. 保存截图作为验收证据

## 13. 已知限制
- 当前模型加载是“通用能力”，不代表项目内已经内置或专门适配“宋卿体育馆”资�?- 多数功能依赖真实外部数据文件
- 视角书签不跨程序保存
- 地形高程查询、对象查询都属于“先切模式，再点击场景”的两步式操�?- 一些实验菜单的成功/失败信息写到�?`UpdateTerrainStatus(...)`，但状态小窗口当前并未正常作为主要反馈界面使用，所以用户会觉得“点了没反应�?- 批量模型总览依赖批量模型图层命名规则；如果图层名不符合预期，功能可能无法定位到模型集

---

## ���θ̲߳�ѯ����ʵ��ͨ�����£�
- ��ǰ����״̬����ʵ��ͨ����
- ��ȷ�������裺
  1. �ȼ��ص�����Ӱ��
  2. ��� ʵ�� -> ���θ̲߳�ѯ��
  3. �������ѽ�����θ̲߳�ѯģʽ�������������α����ѯ�̡߳�����ʾ��
  4. ���������α��棻
  5. �����߳���Ϣ��
- �����������ٰ�����
  - Terrain Query
  - Layer: ����
  - XY
  - Scene Elevation
- ˵������ǰ��ʾ�ĸ߳�ֵ�ǳ����߳� Scene Elevation�������е�ĳ��� Z ֵ������һ����ͬ��ԭʼ DEM �ľ��Ը߳�ֵ��
- �����ܲ��ǵ���˵������̳���������ǡ������ѯģʽ�������������α��桱�ŵ�����